{
  "name": "beacon-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/start.ts",
    "prestart": "bun run build",
    "start": "bun run dist/start.js",
    "build": "bun build --target=bun --outdir=dist src/start.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@contextvm/sdk": "^0.1.33",
    "@modelcontextprotocol/sdk": "^1.20.1",
    "nostr-tools": "^2.17.0",
    "rotating-file-stream": "^3.2.7"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "bun-types": "^1.3.0"
  }
}
